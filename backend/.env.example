MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@your-cluster.mongodb.net/fieldday?retryWrites=true&w=majority
PORT=4000

# Optional: SMTP settings for sending real 2FA emails.
# If left unset, the 2FA code is logged to the server console instead.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
